# API server deps — CPU-only torch keeps the Docker image small.
# For GPU inference in production, swap the torch index URL for a CUDA wheel.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu

fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

opencv-python-headless==4.10.0.84
Pillow==10.3.0
numpy==1.26.4
